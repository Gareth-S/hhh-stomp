diff --git a/assets/lyrics.js b/assets/lyrics.js
index 37d408b..def8a7b 100644
--- a/assets/lyrics.js
+++ b/assets/lyrics.js
@@ -607,6 +607,18 @@ function insertInlineBandNotes(inline)
     }
 }
 
+
+function clearUserNotesDisplay()
+{
+    document
+        .querySelectorAll(".user-note, .user-cue")
+        
+        .forEach(
+            note => note.remove()
+            );
+}
+
+
 /*----------------------------------------------------------*/
 /* Render Inline Band Cue                                   */
 /*----------------------------------------------------------*/
@@ -1262,13 +1274,32 @@ function initialiseNotesEditor()
                         "SAVE RESULT:",
                         result
                     );
+                      
+/*
+                     
+            if (result.success)
+                {
                     
-                    if (result.success)
-                    {
-                        closeNotesEditor();
-                    }
+                    userNotes = notes;
+                    
+                    clearUserNotesDisplay();
+                    
+                    insertUserNotes(userNotes.user, userNotes.inline);
+                    insertTopUserNotes(userNotes.user, userNotes.songNotes);
+                    insertSectionUserNotes(userNotes.user, userNotes.sections);
+
+                    closeNotesEditor();
+                }
+*/
+
+            if (result.success)
+                {
+                    await refreshUserNotes();
+                    closeNotesEditor();
                 }
 
+            }
+                
                 catch (error)
                 {
                     console.error(
@@ -1498,3 +1529,12 @@ for (const row of lineRows)
     return notes;
 }
 
+async function refreshUserNotes()
+{
+    clearUserNotesDisplay();
+
+    userNotes = null;
+
+    await loadUserNotes();
+}
+
